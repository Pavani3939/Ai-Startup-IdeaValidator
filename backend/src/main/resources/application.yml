spring:
  application:
    name: validator
  datasource:
    url: jdbc:mysql://localhost:3306/startup_validator?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: Pavani@3939
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

gemini:
  api:
    key: ${GEMINI_API_KEY:your-default-api-key-if-not-set-in-env}
    url: https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-pro:generateContent

server:
  port: 8080
